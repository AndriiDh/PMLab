spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    root: INFO
    com.example: DEBUG

jwt:
  secret: jwt.super.secret.key.jwt.super.secret.key
  token_life_time: 7

admin:
  email: admin@mail.com
  password: password
